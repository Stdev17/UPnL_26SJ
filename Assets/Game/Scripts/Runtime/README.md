# Runtime layout

Create a folder for each approved GDD feature and keep its components, data types, and tests together. Current SIGNAL RUSH candidates are `Player`, `Combat`, `Combo`, `World`, `Run`, and `UI`.

Create `Core` only when at least two features share the same stable behavior. Do not create empty feature folders, generic managers, services, interfaces, or extension points in advance.
